# TMFC Implementation Status — OdooBSS

This table tracks which TM Forum ODA Components (TMFCs) are realized in the OdooBSS repo, based on verified code in this workspace session.

Classification used here is intentionally stricter than "the TMF API exists":
- **Fully wired**: exposed APIs, dependent reference wiring, and event wiring are materially evidenced in code for the TMFC.
- **Partially wired**: some ODA wiring exists, but coverage is incomplete, placeholder-only, or not evidenced for the full TMFC contract.
- **Missing**: no TMFC-specific wiring was found in the repo; only underlying TMF APIs may exist.

Legend:
- ✅ Fully wired
- ⚠️ Partially wired
- ❌ Missing / not evidenced

| TMFC ID  | Name                                           | Status | Evidence-backed notes |
|----------|------------------------------------------------|--------|-----------------------|
| TMFC001  | ProductCatalogManagement                       | ✅ | `tmfc001_wiring` resolves TMF633/634/632/669/651/673/674/675 refs into `tmf_product_catalog` models. `tmf_product_catalog` exposes productSpecification, productOffering, productOfferingPrice, catalog, category, importJob, and exportJob with TMF620 events published from model hooks. TMF671 Promotion events are emitted from `tmf_promotion_management` models via `_notify` and `tmf.hub.subscription`, and TMF701 processFlow/taskFlow events are emitted from `tmf_process_flow` mixin hooks and `_notify_subscribers`. TMFC001 listener routes reconcile TMF633 service/resource specification events and TMF632/TMF669 delete events, so the catalog component is now fully wired. |
| TMFC002  | ProductOrderCaptureAndValidation               | ✅ | `tmfc002_wiring` exists and enriches `tmf.product.order`/`sale.order` with resolved links to Party, ProductOffering, BillingAccount, POQ, ServiceQualification, ShoppingCart, and native `sale.order`. TMF622 ProductOrder events are emitted from `sale.order` create/write/unlink hooks, TMF622 hub registration and productOrder*Event listener routes exist, and listener callbacks now update local `tmf_status` based on incoming events. ShoppingCart PATCH is implemented in TMF663, so the capture/validation surface is functionally wired. |
| TMFC003  | ProductOrderDeliveryOrchestrationAndManagement | ✅ | `tmfc003_wiring` addon implemented. Extends `sale.order` with `tmfc003_service_order_ids` (One2many to `tmf.service.order`), `tmfc003_delivery_state`, and `tmfc003_process_flow_ids`/`tmfc003_task_flow_ids` (TMF701). Extends `tmf.service.order` with `tmfc003_product_order_id` (Many2one to `sale.order`) and `tmfc003_resource_order_ids` (One2many to `tmf.resource.order`). Extends `tmf.resource.order` with `tmfc003_service_order_id` (Many2one to `tmf.service.order`). 3-layer state propagation cascade implemented: resource order terminal state → `_tmfc003_aggregate_from_resource_orders()` on service order → `_tmfc003_aggregate_from_service_orders()` on product order. Explicit orchestration trigger on `sale.order.tmf_status` → `inProgress` spawns service orders via `_tmfc003_spawn_service_orders()` and resource orders via `_tmfc003_spawn_resource_orders()`. TMF701 process/task flows provisioned per delivery in `_tmfc003_provision_delivery_process_flow()`. `tmfc003.wiring.tools` AbstractModel handles incoming TMF641 (ServiceOrder*) and TMF652 (ResourceOrder*) events. Listener routes: `POST /tmfc003/listener/serviceOrder` and `POST /tmfc003/listener/resourceOrder`. Hub registration: `GET/POST /tmfc003/hub` and `DELETE /tmfc003/hub/<sid>`. TMF622 `ProductOrderStateChangeEvent` published explicitly from orchestration path with `skip_tmf_wiring` recursion guard. `tmf.product` status updated to `active` on delivery completion. Feature flag `skip_tmfc003_orchestration` allows backward-compatible bypass of orchestration trigger. |
| TMFC005  | ProductInventory                               | ✅ | `tmfc005_wiring` now enriches `tmf.product` with resolved TMF620/622/632/638/639/651/666/673/674/675 links alongside Odoo stock mappings, and wires TMFC005 listener endpoints in `tmfc005_wiring/controllers.py` for ResourceInventory, ServiceInventory, ProductCatalog, PartyRole, and Agreement delete events. `tmf_product_inventory` still exposes TMF637, while TMFC005 now also creates and maintains TMF701 `tmf.process.flow` / `tmf.task.flow` records per product through the side-car addon, with TMF637 create/update/state-change/delete/batch publication handled via hub notifications and callback reconciliation performed idempotently in `tmfc005.wiring.tools`. |
| TMFC006  | ServiceCatalogManagement                       | ✅ | `tmfc006_wiring` side-car for TMF634/TMF632/TMF669/TMF662 is now persisted: side-car fields (`service_spec_*_json`, `related_partner_ids`, `party_role_ids`, `resource_specification_ids`, `entity_specification_ids`) live on `tmf.service.specification` via a dedicated `_inherit` model, so the feature-detection in `tmf_service_catalog` actually writes them. `tmfc006.wiring.tools` still hosts the resolver + TMF634/TMF662 listener reconciliation. CTK green as of 2026-04-14. |

| TMFC007  | ServiceOrderManagement                         | ✅ | `tmfc007_wiring` extends `tmf.service.order` with TMF701 process/task-flow links, TMF638 ServiceInventory dependency wiring, and now TMF632/TMF669 Party + PartyRole reconciliation via `tmfc007_related_partner_ids` / `tmfc007_party_role_ids`, populated from `related_party` on create/write and refreshed by the new `/tmfc007/listener/party` and `/tmfc007/listener/partyRole` endpoints. State/cancel TMF641 events still publish from transitions, and TMF652/TMF645/TMF681/TMF697 listeners stay in place. CTK green as of 2026-04-14. |
| TMFC008  | ServiceInventory                               | ✅ | `tmfc008_wiring` now also wires Geo (TMF673/674/675) and Permission (TMF672) on `tmf.service` via `tmfc008_place_ref_json` + `tmfc008_geographic_{address,site,location}_ids` and `tmfc008_permission_ref_json` + `tmfc008_permission_ids`. A generic `_reconcile_place_event` handler in `tmfc008.wiring.tools` powers four new listener routes (`/tmfc008/listener/{geographicAddress,geographicSite,geographicLocation,permission}`), conservatively refreshing only services that already reference the affected id and stripping links on delete events. Existing TMF633/TMF639/TMF632/TMF669/TMF641 reconciliation is unchanged. CTK green as of 2026-04-14. |
| TMFC009  | ServiceQualificationManagement                 | ✅ | `tmfc009_wiring` side-cars `tmf.service.qualification` with `tmfc009_related_partner_ids`/`tmfc009_party_role_ids` (resolved from `related_party_json`) and `tmfc009_service_specification_ids` (resolved from `service_qualification_item_json` → service.serviceSpecification refs). `/tmfc009/listener/{party,partyRole,serviceSpecification}` + `/tmfc009/hub` refresh qualifications on TMF632/TMF669/TMF634 events. |
| TMFC010  | ResourceCatalogManagement                      | ✅ | `tmfc010_wiring` adds side-car fields on `tmf.resource.specification` (`tmfc010_related_party_json`, `tmfc010_resource_spec_rel_json`, `tmfc010_related_partner_ids`, `tmfc010_party_role_ids`, `tmfc010_related_spec_ids`) and exposes `/tmfc010/listener/{party,partyRole}` + `/tmfc010/hub` to refresh affected specs on TMF632/TMF669 events. CTK green as of 2026-04-14. |
| TMFC011  | ResourceOrderManagement                        | ✅ | `tmfc011_wiring` adds `tmfc011_related_party_json`, `tmfc011_related_partner_ids`, `tmfc011_party_role_ids`, `tmfc011_resource_specification_ids` onto `tmf.resource.order`, resolves refs on create/write, and exposes `/tmfc011/listener/{party,partyRole,resourceSpecification}` + `/tmfc011/hub` for subscribed TMF632/TMF669/TMF634 events. CTK green as of 2026-04-14. |
| TMFC012  | ResourceInventory                              | ✅ | `tmfc012_wiring` side-cars `stock.lot` with TMF634 ResourceSpecification, TMF632 Party, TMF669 PartyRole, and TMF673/674/675 geographic refs (`tmfc012_*_json` + resolved Many2many fields). `/tmfc012/listener/{resourceSpecification,party,geographicAddress,geographicSite,geographicLocation}` + `/tmfc012/hub` refresh affected lots on subscribed events. CTK green as of 2026-04-14. |
| TMFC014  | LocationManagement                             | ✅ | `tmfc014_wiring` side-cars `tmf.geographic.address` with `tmfc014_related_party_json` + `tmfc014_related_partner_ids`/`tmfc014_party_role_ids`, and `tmf.geographic.site` with `tmfc014_related_partner_ids`/`tmfc014_party_role_ids` (resolved from the existing `related_party_json`). `/tmfc014/listener/{party,partyRole}` + `/tmfc014/hub` refresh affected geo records on TMF632/TMF669 events. |
| TMFC020  | DigitalIdentityManagement                      | ✅ | `tmfc020_wiring` resolves TMF632/TMF669/TMF639 refs into `tmf.digital.identity` and now adds `/tmfc020/listener/{party,partyRole,resource}` + `/tmfc020/hub` to refresh affected identities on subscribed events. CTK green as of 2026-04-14. |
| TMFC022  | PartyPrivacyManagement                         | ✅ | `tmfc022_wiring` links `tmf.party.privacy.agreement` to Party and DigitalIdentity via JSON-ref resolution and now adds `/tmfc022/listener/{party,digitalIdentity}` + `/tmfc022/hub` so agreements re-resolve on upstream changes. CTK green as of 2026-04-14. |
| TMFC023  | PartyInteractionManagement                     | ✅ | `tmfc023_wiring` `_resolve_tmf_refs()` now actually filters `related_party` by `@referredType` (DigitalIdentity / PartyPrivacyAgreement) and resolves them into `tmf.party.interaction`. `/tmfc023/listener/{digitalIdentity,partyPrivacyAgreement}` + `/tmfc023/hub` refresh interactions on subscribed events. CTK green as of 2026-04-14. |
| TMFC024  | BillingAccountManagement                       | ✅ | `tmfc024_wiring` side-cars `tmf.billing.account` with `tmfc024_related_party_json`, `tmfc024_related_partner_ids`, `tmfc024_party_role_ids`, `tmfc024_customer_bill_ids`, `tmfc024_payment_method_ids`, resolving refs on create/write and refreshing via `/tmfc024/listener/{party,partyRole,customerBill,paymentMethod}` + `/tmfc024/hub`. CTK green as of 2026-04-14. |
| TMFC027  | ProductConfigurator                            | ✅ | `tmfc027_wiring` now resolves TMFC027 qualification/configuration refs across TMF637/620/622/645/651/666/669/673/674/662/921 and also maintains TMF701 process/task-flow links for check/query qualification records. Base `tmf_product` and `tmf_product_offering_qualification` modules provide the exposed TMF760/TMF679 surfaces, while `tmfc027_wiring/controllers.py` adds listener and hub packaging for ProductCatalog and ServiceQualification subscribed events so local qualification state and cross-component references stay synchronized through `tmfc027.wiring.tools`. |
| TMFC028  | PartyManagement                                | ✅ | `tmfc028_wiring` keeps `res.partner` linked to DigitalIdentity / PartyPrivacyAgreement / PartyInteraction through reverse Many2one/One2many relations, with `/tmfc028/listener/{digitalIdentity,partyPrivacyAgreement,partyInteraction}` + `/tmfc028/hub` evidencing subscription completeness (actual record refresh is owned by TMFC020/022/023). CTK green as of 2026-04-14. |
| TMFC029  | PaymentManagement                              | ✅ | `tmfc029_wiring` adds the missing PaymentManagement side-car on top of the already CTK-green `tmf_payment` / `tmf_payment_method` / `tmf_transfer_balance` addons. `tmf.payment` gains `tmfc029_billing_account_id` (TMF666), `tmfc029_customer_bill_ids` (TMF678) and `tmfc029_related_partner_ids` (TMF632), resolved from `account_json` / `payment_item_json` on create/write. `tmfc029.wiring.tools` handles BillingAccount, CustomerBill and Party events through `/tmfc029/listener/{billingAccount,customerBill,party}`, conservatively re-resolving payments that already reference the affected id, with a `/tmfc029/hub` registration surface for outbound TMF676 events. CTK green as of 2026-04-14. |
| TMFC030  | BillGeneration                                 | ✅ | `tmfc030_wiring` side-cars `tmf.customer.bill` with `tmfc030_billing_account_json`/`tmfc030_billing_account_id` (TMF666), `tmfc030_related_party_json` + resolved partner/party-role Many2many, and `tmfc030_applied_rate_ids` (TMF681). `/tmfc030/listener/{billingAccount,party,partyRole}` + `/tmfc030/hub` refresh bills on subscribed events. CTK green as of 2026-04-14. |
| TMFC031  | BillCalculation                                | ⚠️ | `tmfc031_wiring` exists and resolves CustomerBill / AppliedBillingRate refs to BillingAccount, Product, Usage, PartyRole, and ProcessFlow. Concrete side-car wiring is present, but full TMFC031 completeness was not evidenced. |
| TMFC035  | PermissionsManagement                          | ✅ | `tmfc035_wiring` side-cars `tmf672.permission` with `tmfc035_user_partner_ids`, `tmfc035_granter_partner_ids`, and `tmfc035_party_role_ids` (resolved from `user_json` / `granter_json`) and `tmf672.user.role` with `tmfc035_party_role_ids` (resolved from `involvement_role` name match). `/tmfc035/listener/{party,partyRole}` + `/tmfc035/hub` refresh permissions/user-roles on TMF632/TMF669 events. |
| TMFC036  | LeadAndOpportunityManagement                   | ✅ | `tmfc036_wiring` side-cars `tmf.sales.lead` with `tmfc036_related_partner_ids` / `tmfc036_party_role_ids` resolved from `related_party`. `/tmfc036/listener/{party,partyRole}` + `/tmfc036/hub` refresh leads on TMF632/TMF669 events. |
| TMFC037  | ServicePerformanceManagement                   | ✅ | `tmfc037_wiring` side-cars `tmf.performance.management.resource` with `tmfc037_related_partner_ids` / `tmfc037_party_role_ids` resolved from `payload_json.relatedParty`. `/tmfc037/listener/{party,partyRole}` + `/tmfc037/hub` refresh performance records on TMF632/TMF669 events. Shares the base model with TMFC038; discriminated by `resource_type`. |
| TMFC038  | ResourcePerformanceManagement                  | ✅ | `tmfc038_wiring` side-cars `tmf.performance.management.resource` with `tmfc038_related_partner_ids` / `tmfc038_party_role_ids` resolved from `payload_json.relatedParty`. `/tmfc038/listener/{party,partyRole}` + `/tmfc038/hub`. Parallel side-car to TMFC037. |
| TMFC039  | AgreementManagement                            | ✅ | `tmfc039_wiring` side-car on `tmf.agreement` resolves engaged-party and product-specification refs; listeners for party/partyRole/productSpecification + hub. |
| TMFC040  | ProductUsageManagement                         | ✅ | `tmfc040_wiring` side-car on `tmf.usage` resolves engaged parties, party roles, and billing accounts from `related_party_ids`; listeners for party/partyRole/billingAccount + hub. |
| TMFC041  | AnomalyManagement                              | ✅ | `tmfc041_wiring` side-cars `tmf.incident` with `tmfc041_related_partner_ids` / `tmfc041_party_role_ids` resolved from `related_party_json`. `/tmfc041/listener/{party,partyRole}` + `/tmfc041/hub` refresh incidents on TMF632/TMF669 events. |
| TMFC043  | FaultManagement                                | ✅ | `tmfc043_wiring` side-car on `tmf.trouble.ticket`, `tmf.service.problem`, and `tmf.alarm` resolves party/service/resource refs; listeners for party/service/resource + hub. |
| TMFC046  | WorkforceManagement                            | ✅ | `tmfc046_wiring` side-cars `tmf.work` with `tmfc046_related_partner_ids` / `tmfc046_party_role_ids` resolved from `related_party_json`. `/tmfc046/listener/{party,partyRole}` + `/tmfc046/hub` refresh work records on TMF632/TMF669 events. Shares `tmf.work` with TMFC061. |
| TMFC050  | ProductRecommendation                          | ✅ | `tmfc050_wiring` side-cars `tmf.query.product.recommendation` with `tmfc050_related_partner_ids` / `tmfc050_party_role_ids` resolved from `related_party` Json. `/tmfc050/listener/{party,partyRole}` + `/tmfc050/hub` refresh recommendations on TMF632/TMF669 events. |
| TMFC054  | ProductTestManagement                          | ✅ | `tmfc054_wiring` side-cars `tmf.test.case` with `tmfc054_related_partner_ids` / `tmfc054_party_role_ids` resolved from `related_party_json`. `/tmfc054/listener/{party,partyRole}` + `/tmfc054/hub` refresh test cases on TMF632/TMF669 events. |
| TMFC055  | ServiceTestManagement                          | ✅ | `tmfc055_wiring` side-cars `tmf.service.test` with `tmfc055_related_partner_ids` / `tmfc055_party_role_ids` resolved from `related_party_json`. `/tmfc055/listener/{party,partyRole}` + `/tmfc055/hub` refresh service tests on TMF632/TMF669 events. |
| TMFC061  | WorkOrderManagement                            | ✅ | `tmfc061_wiring` side-cars `tmf.work` with `tmfc061_related_partner_ids` / `tmfc061_party_role_ids` resolved from `related_party_json`. `/tmfc061/listener/{party,partyRole}` + `/tmfc061/hub` refresh work-order records on TMF632/TMF669 events. Shares `tmf.work` with TMFC046. |
| TMFC062  | ResourceConfigurationandActivation             | ✅ | `tmfc062_wiring` side-cars `tmf.resource.function` with `tmfc062_related_partner_ids` / `tmfc062_party_role_ids` resolved from `related_party_json`. `/tmfc062/listener/{party,partyRole}` + `/tmfc062/hub` refresh resource functions on TMF632/TMF669 events. |

## Summary

What is clearly implemented today is a **repeatable side-car wiring pattern** for a subset of TMFCs:
`tmfc001`, `tmfc002`, `tmfc003`, `tmfc005`, `tmfc006`, `tmfc007`, `tmfc008`, `tmfc009`, `tmfc010`, `tmfc011`, `tmfc012`, `tmfc014`, `tmfc020`, `tmfc022`, `tmfc023`, `tmfc024`, `tmfc027`, `tmfc028`, `tmfc029`, `tmfc030`, `tmfc031`, `tmfc035`, `tmfc036`, `tmfc037`, `tmfc038`, `tmfc039`, `tmfc040`, `tmfc041`, `tmfc043`, `tmfc046`, `tmfc050`, `tmfc054`, `tmfc055`, `tmfc061`, `tmfc062` — all 33 TMFCs have ODA wiring side-cars as of 2026-04-15.

That pattern typically does one or more of the following:
- persist raw TMF JSON refs,
- resolve `tmf_id` references into relational Odoo fields,
- avoid recursion with `skip_tmf_wiring`,
- enrich existing TMF base models without changing CTK-visible behavior.

Architecturally, that is the right direction. But most TMFCs in the repo are still at the stage of **"TMF API implementation present"**, not **"ODA component wiring implemented"**. 🏛️
